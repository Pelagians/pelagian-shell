schema_version = 1

[layout]
mode = "auto"
solo = "maximized"
multiple = "automatic"
dialogs = "floating"
max_managed_windows = 6

[decorations]
solo = "full"
tiled = "full"
floating = "full"

[theme]
variant = "dark"

[capabilities]
wine = false
